{
  "openapi": "3.0.0",
  "info": { "title": "Treasury API", "version": "1.0.0", "description": "Treasury endpoints (fa/en)" },
  "paths": {
    "/api/treasury/accounts": { "get": { "summary": "List accounts", "responses": { "200": { "description": "OK" } } } },
    "/api/treasury/transfer": { "post": { "summary": "Create transfer", "requestBody": { "content": { "application/json": { "schema": { "type": "object" } } } }, "responses": { "200": { "description": "OK" } } } }
  }
}
